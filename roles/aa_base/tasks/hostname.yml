---

- name: Set hostname
  ansible.builtin.hostname:
      name: '{{ inventory_hostname }}'

- name: Entry in /etc/hosts
  ansible.builtin.lineinfile:
      path: /etc/hosts
      line: 127.0.0.2 {{ ansible_nodename }} {{ ansible_hostname }}
